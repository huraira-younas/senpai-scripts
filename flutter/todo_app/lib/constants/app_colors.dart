import 'package:flutter/material.dart' show Color, Colors;

class AppColors {
  static final grayColor = Colors.grey.shade600;
  static const primaryColor = Color(0xFF0AB6AB);
  static const whiteColor = Colors.white;
  static const bgColor = Color(0xFF151515);
}
